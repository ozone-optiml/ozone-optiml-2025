# Preparation codes for preprocessing the raw netcdf files

- ``Obs`` are t
- Handles ``CMAQ, MCIP, Obs, CMAQ_Reanalysis``.
- Saves the preprocessed data into ``InputData``, which is used to generate train/test data